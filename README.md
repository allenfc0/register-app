# register-app

This application is a register application that will process transactions made in a business. These transactions will be stored in a relational database so that the business can readily access data for whatever needs. 
